# auto-generated by grc.converter

id: radar_estimator_sync_pulse_c
label: Estimator Sync Pulse
category: '[RADAR]/Estimators'

parameters:
-   id: num_xcorr
    label: Number cross correlations
    dtype: int
-   id: len_key
    label: Packet length key
    dtype: string
    default: '"packet_len"'

inputs:
-   label: IQ in TX
    domain: stream
    dtype: complex
-   label: IQ in RX
    domain: stream
    dtype: complex

outputs:
-   domain: message
    id: Msg out
    optional: true

templates:
    imports: import radar
    make: radar.estimator_sync_pulse_c(${num_xcorr}, ${len_key})
    callbacks:
    - set_num_xcorr(${num_xcorr})

file_format: 1
