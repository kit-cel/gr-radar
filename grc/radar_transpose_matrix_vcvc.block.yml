# auto-generated by grc.converter

id: radar_transpose_matrix_vcvc
label: Matrix Transpose
category: '[RADAR]/Tools'

parameters:
-   id: vlen_in
    label: Vector length input
    dtype: int
-   id: vlen_out
    label: Vector length output
    dtype: int
-   id: len_key
    label: Packet length key
    dtype: string
    default: '"packet_len"'

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ vlen_in }

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ vlen_out }

templates:
    imports: import radar
    make: radar.transpose_matrix_vcvc(${vlen_in}, ${vlen_out}, ${len_key})

file_format: 1
