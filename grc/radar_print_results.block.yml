# auto-generated by grc.converter

id: radar_print_results
label: Print Results
category: '[RADAR]/Tools'

parameters:
-   id: store_msg
    label: Store messages
    dtype: bool
    default: 'False'
    options: ['True', 'False']
-   id: filename
    label: Filename
    dtype: string
    default: '""'
    hide: part

inputs:
-   domain: message
    id: Msg in
    optional: true

templates:
    imports: import radar
    make: radar.print_results(${store_msg}, ${filename})

file_format: 1
