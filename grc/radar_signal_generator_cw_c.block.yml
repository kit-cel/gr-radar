# auto-generated by grc.converter

id: radar_signal_generator_cw_c
label: Signal Generator CW
category: '[RADAR]/Generators'

parameters:
-   id: packet_len
    label: Packet length
    dtype: int
-   id: samp_rate
    label: Sample rate
    dtype: int
-   id: frequency
    label: Frequency
    dtype: real_vector
-   id: amplitude
    label: Amplitude
    dtype: float
-   id: len_key
    label: Packet length key
    dtype: string
    default: '"packet_len"'

outputs:
-   label: IQ out
    domain: stream
    dtype: complex

templates:
    imports: import radar
    make: radar.signal_generator_cw_c(${packet_len}, ${samp_rate}, ${frequency}, ${amplitude},
        ${len_key})

file_format: 1
