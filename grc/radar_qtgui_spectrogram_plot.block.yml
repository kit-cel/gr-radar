# auto-generated by grc.converter

id: radar_qtgui_spectrogram_plot
label: QT GUI Spectrogram Plot
category: '[RADAR]/GUI'

parameters:
-   id: vlen
    label: Input vector length
    dtype: int
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: interval
    label: Update interval [ms]
    dtype: int
-   id: xlabel
    label: Label x
    dtype: string
-   id: ylabel
    label: Label y
    dtype: string
-   id: label
    label: Label title
    dtype: string
-   id: axis_x
    label: Axis x
    dtype: real_vector
-   id: axis_y
    label: Axis y
    dtype: real_vector
-   id: axis_z
    label: Axis z
    dtype: real_vector
-   id: autoscale_z
    label: Autoscale z axis
    dtype: bool
    default: 'True'
    options: ['True', 'False']
-   id: len_key
    label: Packet length key
    dtype: string
    default: '"packet_len"'

inputs:
-   label: IQ in
    domain: stream
    dtype: float
    vlen: ${ vlen }

templates:
    imports: import radar
    make: radar.qtgui_spectrogram_plot(${vlen}, ${interval}, ${xlabel}, ${ylabel},
        ${label}, ${axis_x}, ${axis_y}, ${axis_z}, ${autoscale_z}, ${len_key})

file_format: 1
