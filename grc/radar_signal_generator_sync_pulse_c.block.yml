# auto-generated by grc.converter

id: radar_signal_generator_sync_pulse_c
label: Signal Generator Sync Pulse
category: '[RADAR]/Generators'

parameters:
-   id: packet_len
    label: Packet length
    dtype: int
-   id: pulse_len
    label: Send pulse length
    dtype: int_vector
-   id: pulse_pause
    label: Wait pulse length
    dtype: int_vector
-   id: pulse_amplitude
    label: Pulse amplitude
    dtype: float
-   id: len_key
    label: Packet length key
    dtype: string
    default: '"packet_len"'

outputs:
-   label: IQ out
    domain: stream
    dtype: complex

templates:
    imports: import radar
    make: radar.signal_generator_sync_pulse_c(${packet_len}, ${pulse_len}, ${pulse_pause},
        ${pulse_amplitude}, ${len_key})

file_format: 1
