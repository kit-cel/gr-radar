# auto-generated by grc.converter

id: radar_ofdm_cyclic_prefix_remover_cvc
label: OFDM Cyclic Prefix Remover
category: '[RADAR]/RADAR'

parameters:
-   id: fft_len
    label: FFT length
    dtype: int
-   id: cp_len
    label: CP length
    dtype: int
-   id: len_key
    label: Packet length key
    dtype: string
    default: '"packet_len"'

inputs:
-   label: IQ in
    domain: stream
    dtype: complex

outputs:
-   label: IQ out
    domain: stream
    dtype: complex
    vlen: ${ fft_len }

templates:
    imports: import radar
    make: radar.ofdm_cyclic_prefix_remover_cvc(${fft_len}, ${cp_len}, ${len_key})

file_format: 1
