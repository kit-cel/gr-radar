<?xml version="1.0"?>
<block>
  <name>Tagged Stream Cross Correlate</name>
  <key>radar_ts_cross_correlate_cvc</key>
  <category>RADAR</category>
  <import>import radar</import>
  <make>radar.ts_cross_correlate_cvc($packet_len, $len_key)</make>
  <param>
    <name>Packet length</name>
    <key>packet_len</key>
    <type>int</type>
  </param>
  <param>
    <name>Packet length key</name>
    <key>len_key</key>
    <value> "packet_len"</value>
    <type>string</type>
  </param>

  <!-- Make one 'sink' node per input. Sub-nodes:
       * name (an identifier for the GUI)
       * type
       * vlen
       * optional (set to 1 for optional inputs) -->
  <sink>
    <name>Reference in</name>
    <type>complex</type>
  </sink>
  <sink>
    <name>Echo in</name>
    <type>complex</type>
  </sink>

  <!-- Make one 'source' node per output. Sub-nodes:
       * name (an identifier for the GUI)
       * type
       * vlen
       * optional (set to 1 for optional inputs) -->
  <source>
    <name>out</name>
    <type>complex</type>
    <vlen>1</vlen>
  </source>
</block>
