# auto-generated by grc.converter

id: radar_split_cc
label: Split
category: '[RADAR]/Tools'

parameters:
-   id: packet_num
    label: Packet number
    dtype: int
-   id: packet_parts
    label: Packet parts
    dtype: real_vector
-   id: len_key
    label: Packet length key
    dtype: string
    default: '"packet_len"'

inputs:
-   label: IQ in
    domain: stream
    dtype: complex

outputs:
-   label: IQ out
    domain: stream
    dtype: complex

templates:
    imports: import radar
    make: radar.split_cc(${packet_num}, ${packet_parts}, ${len_key})

file_format: 1
