# auto-generated by grc.converter

id: radar_ts_fft_cc
label: Tagged Stream FFT
category: '[RADAR]/Tools'

parameters:
-   id: packet_len
    label: Packet length
    dtype: int
-   id: len_key
    label: Packet length key
    dtype: string
    default: ' "packet_len"'

inputs:
-   label: IQ in
    domain: stream
    dtype: complex

outputs:
-   label: IQ out
    domain: stream
    dtype: complex

templates:
    imports: import radar
    make: radar.ts_fft_cc(${packet_len}, ${len_key})

file_format: 1
