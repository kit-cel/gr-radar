# auto-generated by grc.converter

id: radar_trigger_command
label: Trigger Command
category: '[RADAR]/Tools'

parameters:
-   id: command
    label: Command
    dtype: string
-   id: identifiers
    label: Identifiers
    dtype: raw
-   id: vals_min
    label: Value min
    dtype: real_vector
-   id: vals_max
    label: Value max
    dtype: real_vector
-   id: block_time
    label: Block duration [ms]
    dtype: int
    default: '0'

inputs:
-   domain: message
    id: Msg in
    optional: true

templates:
    imports: import radar
    make: radar.trigger_command(${command}, ${identifiers}, ${vals_min}, ${vals_max},
        ${block_time})

file_format: 1
